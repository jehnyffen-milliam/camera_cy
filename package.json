{
  "name": "camera_cy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cy:run:smoke": "docker run -it -v $PWD:/e2e -w /e2e --config integrationFolder=cypress/integration"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.4.0"
  }
}
